# Use PyPI plus PyTorch CUDA wheel index. When installing, use:
#   pip install -r requirements.txt
# The extra index provides CUDA-enabled wheels for torch.
--extra-index-url https://download.pytorch.org/whl/cu121

# Core runtime
-r requirements-common.txt

# PyTorch (CUDA 12.1) - pinned to known compatible wheel
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# Optional tooling
streamlit
pandas
altair
